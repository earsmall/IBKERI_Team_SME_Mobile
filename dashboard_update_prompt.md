# 한국 중소기업 요약노트 업데이트 프롬프트

현재 프로젝트를 수정할 때는 아래 상태를 기준으로 이어서 작업한다.

## 프로젝트 개요

- 모바일 화면에 최적화된 정적 대시보드 웹페이지다.
- 상단 브랜드는 `IBK경제연구소`, 메인 타이틀은 `한국 중소기업 요약노트`다.
- 상단 탭은 `위상`, `실물경기`, `체감경기`, `경영지표`, `대출`, `투자`, `수출`, `연간 창업` 순서다.
- 전체 디자인은 밝은 블루 톤, 흰색 카드, 둥근 모서리, 은은한 그림자를 유지한다.
- 폰트는 `IBM Plex Sans KR`을 사용한다.

## 파일 구조

- `index.html`: 탭 구조와 섹션 마크업
- `styles.css`: 전체 스타일
- `script.js`: 데이터 로딩, 파싱, 렌더링, 새로고침 로직
- `img/re.png`: 새로고침 버튼 이미지

## 데이터 소스와 새로고침 규칙

### OpenAPI 기반 탭

아래 탭은 원본 통계 페이지 데이터가 업데이트되면, 해당 탭의 `새로고침` 버튼을 눌러 최신 데이터를 다시 불러오도록 구현한다.

- `위상`
  - KOSIS OpenAPI 사용
  - `기업수`, `종사자수`, `매출액` 3개 섹션 각각 새로고침 버튼 존재
- `실물경기`
  - KOSIS OpenAPI 사용
  - `중소기업 경기동행종합지수`, `중소기업 생산지수`, `중소제조업 평균가동률` 섹션별 새로고침 버튼 존재
- `체감경기`
  - KOSIS OpenAPI 사용
  - `중소기업 BSI` 섹션 새로고침 버튼 존재
- `경영지표`
  - KOSIS OpenAPI 사용
  - `성장성`, `수익성`, `안정성`을 한 번에 다시 읽는 새로고침 버튼 존재
- `수출`
  - KOSIS OpenAPI 사용
  - `중소기업 수출` 섹션 새로고침 버튼 존재
- `연간 창업`
  - KOSIS OpenAPI 사용
  - `연간 창업` 섹션 새로고침 버튼 존재

### 구글 시트 기반 탭

아래 탭은 구글 시트 데이터가 갱신되면, 해당 탭의 `새로고침` 버튼을 눌러 최신 시트를 다시 읽도록 구현한다.

- `대출`
  - 구글 시트 사용
  - `대출` 섹션 새로고침 버튼 존재
- `투자`
  - 구글 시트 사용
  - `투자` 섹션 새로고침 버튼 존재

## 탭별 현재 기준

### 1. 위상

- 데이터 소스: KOSIS OpenAPI
- 기준: 전산업
- 섹션:
  - `기업수`
  - `종사자수`
  - `매출액`
- 각 섹션 구성:
  - 헤더 우측 `기준년도` 드롭다운
  - 헤더 우측 `새로고침` 버튼
  - 도넛 카드
  - 최근 3년 추이 막대그래프
  - 중소기업 비중 라인그래프
  - 업종별 비중 상위 5개
- 출처 표기:
  - `자료: 중소벤처기업부`
  - `《중소기업 기본통계》`

### 2. 실물경기

- 데이터 소스: KOSIS OpenAPI
- 섹션:
  - `중소기업 경기동행종합지수`
  - `중소기업 생산지수`
  - `중소제조업 평균가동률`
- 출처는 사용자가 지정한 문구를 유지한다.

### 3. 체감경기

- 데이터 소스: KOSIS OpenAPI
- 헤더: `중소기업 BSI`
- `BSI코드` 드롭다운 선택 가능
- 같은 코드에 대해 `실적`과 `전망`을 같이 보여준다.
- 출처: `자료: 한국은행`

### 4. 경영지표

- 데이터 소스: KOSIS OpenAPI
- 헤더:
  - `성장성 지표: 매출액 증가율`
  - `수익성 지표: 매출액영업이익률`
  - `안정성 지표: 부채비율`
- 상단 헤더 우측에 `기준연도` 드롭다운과 `새로고침` 버튼이 있다.
- 새로고침 시 3개 지표를 모두 다시 읽는다.
- 출처: `자료: 한국은행 《기업경영분석》`

### 5. 대출

- 데이터 소스: 구글 시트
- 헤더 우측 `기준년도` 드롭다운과 `새로고침` 버튼 존재
- 새로고침 시 대출 본표와 연체율 시트를 다시 읽는다.

### 6. 투자

- 데이터 소스: 구글 시트
- 헤더 우측 `기준시점` 드롭다운과 `새로고침` 버튼 존재
- 새로고침 시 `투자`, `업력별투자`, `업종별투자`, `출자자별` 시트를 다시 읽는다.

### 7. 수출

- 데이터 소스: KOSIS OpenAPI
- 헤더 구분:
  - `중소기업 수출`
  - `중소기업 주요 수출국`
- 헤더 우측 `기준시점` 드롭다운과 `새로고침` 버튼 존재
- 출처:
  - `자료: 국가데이터처`
  - `《기업특성별무역통계》`

### 8. 연간 창업

- 데이터 소스: KOSIS OpenAPI
- 헤더: `연간 창업`
- 헤더 우측 `기준년도` 드롭다운과 `새로고침` 버튼 존재
- 섹션 순서:
  - `창업기업 수`
  - `기술기반업종 창업`
  - `업종별 창업 비중 Top-5`
- `기술기반업종 창업기업 수`는 별도 `기술기반업종` 행을 직접 사용한다.
- 서비스업 세부 업종은 `상위 5개 업종` 계산 시 다른 업종과 같은 레벨로 본다.
- 출처:
  - `자료: 중소벤처기업부`
  - `《창업기업동향》`

## 구현 원칙

- 사용자가 정한 탭명, 헤더명, 출처 문구, 차트 순서를 임의로 바꾸지 않는다.
- OpenAPI 기반 탭은 가능한 한 `직접 호출 -> 프록시 재시도 -> 로컬 캐시` 순서로 안정적으로 동작하게 유지한다.
- 구글 시트 기반 탭은 `새로고침` 버튼으로 실제 시트를 다시 읽도록 유지한다.
- 모바일 가독성을 해치지 않는 선에서만 시각 요소를 조정한다.
- HTML, CSS, JS를 함께 맞춰서 수정한다.

## 작업 전 확인할 것

- `index.html`에서 탭/헤더/버튼 위치 확인
- `styles.css`에서 섹션 공통 스타일과 카드/차트 스타일 확인
- `script.js`에서 해당 탭의 로더, 파서, 렌더러, 새로고침 바인딩 확인

## 작업 후 확인할 것

- 탭 전환이 정상인지
- 각 드롭다운 선택이 카드와 차트에 반영되는지
- OpenAPI 기반 탭의 새로고침 버튼이 최신 API 재호출로 연결되는지
- 구글 시트 기반 탭의 새로고침 버튼이 최신 시트 재호출로 연결되는지
- 오류 시 안내 문구가 자연스러운지
